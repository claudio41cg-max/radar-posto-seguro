/* Radar Seguro RJ PRO v194 — ambiente TomTom puro para rota.
   Toda a geometria, tempo e instrucoes da rota vem da TomTom. */
(()=>{
'use strict';
if(window.__radarRoutingTomTomV194)return;
window.__radarRoutingTomTomV194=true;
const WORKER='https://radar-seguro-ia-rj.claudio41cg.workers.dev';
const app=()=>{try{return window.RadarApp||window.App||null}catch(_){return null}};
const point=p=>Array.isArray(p)&&p.length>=2&&Number.isFinite(+p[0])&&Number.isFinite(+p[1]);
const rad=d=>d*Math.PI/180,deg=r=>r*180/Math.PI;
function distM(a,b){if(!point(a)||!point(b))return Infinity;const R=6371000,p1=rad(+a[1]),p2=rad(+b[1]),d1=p2-p1,d2=rad(+b[0]-+a[0]);const q=Math.sin(d1/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(d2/2)**2;return 2*R*Math.asin(Math.min(1,Math.sqrt(q)));}
function bearing(a,b){if(!point(a)||!point(b))return null;const p1=rad(+a[1]),p2=rad(+b[1]),dl=rad(+b[0]-+a[0]),y=Math.sin(dl)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl);return(deg(Math.atan2(y,x))+360)%360;}
function angleDiff(a,b){return Math.abs((((a-b)+540)%360)-180);}
function currentOrigin(a,fallback){const acc=Math.max(0,+a?.currentAccuracy||999);if(point(a?.rawUserPos)&&acc<=90)return[+a.rawUserPos[0],+a.rawUserPos[1]];if(point(a?.filteredPos))return[+a.filteredPos[0],+a.filteredPos[1]];if(point(a?.rawUserPos))return[+a.rawUserPos[0],+a.rawUserPos[1]];if(point(a?.userPos))return[+a.userPos[0],+a.userPos[1]];return point(fallback)?[+fallback[0],+fallback[1]]:null;}
function vehicleHeading(a){const h=+a?.currentBearing,speed=Math.max(0,+a?.currentSpeed||0),acc=Math.max(0,+a?.currentAccuracy||999);return speed>=8&&acc<=60&&Number.isFinite(h)?Math.round((h+360)%360):null;}
function lookupAccess(a,dest){const map=a?.__v194AccessIndex||a?.__v193AccessIndex||a?.__v191AccessIndex;if(!map||!point(dest))return null;const direct=map.get(Number(dest[0]).toFixed(5)+','+Number(dest[1]).toFixed(5));if(direct)return direct;let best=null,bd=Infinity;for(const v of map.values()){const d=distM(dest,v.center);if(d<bd&&d<=45){bd=d;best=v;}}return best;}
function destinationPoint(a,dest){const access=lookupAccess(a,dest);if(access?.entryPoints?.length){const main=access.entryPoints.find(e=>String(e.type||'').toLowerCase()==='main')||access.entryPoints[0],p=[+main.lon,+main.lat];if(point(p))return{point:p,accessMeta:{name:access.name,display:access.display,kind:String(main.type||'entry'),source:access.source||''}};}return{point:dest.slice(),accessMeta:null};}
function modifier(type){const t=String(type||'').toUpperCase();if(t.includes('LEFT'))return'left';if(t.includes('RIGHT'))return'right';if(t.includes('ROUNDABOUT'))return'roundabout';if(t.includes('UTURN')||t.includes('TURN_AROUND'))return'uturn';return'straight';}
function coords(rt){const out=[];for(const leg of rt?.legs||[])for(const p of leg?.points||[]){const c=[+p.longitude,+p.latitude],z=out[out.length-1];if(c.every(Number.isFinite)&&(!z||distM(z,c)>.25))out.push(c);}return out;}
function badge(){let b=document.getElementById('routeProviderV194');if(!b){b=document.createElement('div');b.id='routeProviderV194';Object.assign(b.style,{position:'fixed',right:'10px',bottom:'150px',zIndex:'2147480000',padding:'5px 8px',borderRadius:'999px',font:'700 10px Arial,sans-serif',letterSpacing:'.3px',boxShadow:'0 3px 12px rgba(0,0,0,.35)',pointerEvents:'none',background:'rgba(0,100,180,.92)',color:'#fff'});document.body.appendChild(b);}b.textContent='ROTA • TOMTOM';b.style.display='block';}
async function request(a,origin,requestedDest){const chosen=destinationPoint(a,requestedDest),dest=chosen.point,mode=a.transportMode==='motorcycle'?'motorcycle':'car',params=new URLSearchParams({traffic:'true',travelMode:mode,instructionsType:'text',language:'pt-BR',routeType:'fastest',avoid:'unpavedRoads',routeRepresentation:'polyline',computeTravelTimeFor:'all',maxAlternatives:'2'}),h=vehicleHeading(a);if(h!=null)params.set('vehicleHeading',String(h));const path=`/routing/1/calculateRoute/${origin[1].toFixed(6)},${origin[0].toFixed(6)}:${dest[1].toFixed(6)},${dest[0].toFixed(6)}/json?${params.toString()}`,ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),12000);try{const r=await fetch(`${WORKER}/v1/tomtom?path=${encodeURIComponent(path)}`,{signal:ctl.signal,cache:'no-store',headers:{Accept:'application/json'}});if(!r.ok)throw new Error('TomTom HTTP '+r.status);const j=await r.json(),rt=j?.routes?.[0];if(!rt)throw new Error('TomTom sem rota');const c=coords(rt);if(c.length<2)throw new Error('TomTom sem geometria');const first=c.find(p=>distM(origin,p)>=25)||c[1],fb=bearing(origin,first),hd=h!=null&&fb!=null?angleDiff(h,fb):0;if(h!=null&&Math.max(0,+a.currentSpeed||0)>=10&&hd>155)throw new Error('TomTom iniciou em sentido incompatível com o veiculo');const steps=(rt.guidance?.instructions||[]).map(i=>({name:i.street||i.roadNumbers?.[0]||'Siga pela via',instruction:i.message||'',maneuver:{type:i.maneuver||'',modifier:modifier(i.maneuver),location:[+i.point?.longitude,+i.point?.latitude]},routeOffsetMeters:Number(i.routeOffsetInMeters)||0}));const route={coords:c,steps,distance:+rt.summary?.lengthInMeters||0,duration:+rt.summary?.travelTimeInSeconds||0,time:+rt.summary?.travelTimeInSeconds||0,trafficDelaySeconds:+rt.summary?.trafficDelayInSeconds||0,liveTraffic:true,engine:'tomtom',provider:'TomTom',routingVersion:'194-tomtom',originUsed:origin.slice(),requestedOrigin:origin.slice(),destinationUsed:dest.slice(),requestedDestination:requestedDest.slice(),accessMeta:chosen.accessMeta||null,tomtomRoute:rt,tomtomAlternatives:(j?.routes||[]).slice(1)};try{a.prepareRouteGeometry?.(route);}catch(_){}return route;}finally{clearTimeout(timer);}}
function install(){const a=app();if(!a?.map)return false;if(a.__routingTomTomV194Installed)return true;a.__routingTomTomV194Installed=true;a.getRoute=async function(start,end){const origin=currentOrigin(this,start),dest=point(end)?[+end[0],+end[1]]:null;if(!origin||!dest)throw new Error('Aguardando GPS ou destino');if(typeof navigator!=='undefined'&&navigator.onLine===false)throw new Error('Sem internet para calcular nova rota');const r=await request(this,origin,dest);badge();this.toast?.('Rota calculada pela TomTom.',1600);return r;};return true;}
let tries=0,t=setInterval(()=>{tries++;if(install()||tries>320)clearInterval(t);},100);
window.RadarRoutingTomTomV194={version:'194',route:(s,e)=>{const a=app();return a?.getRoute?.(s,e);}};
})();
