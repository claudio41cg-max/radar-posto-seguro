/* Radar Seguro RJ PRO v117 — mostra até três rotas alternativas clicáveis */
(()=>{'use strict';if(window.__radarAlternativesV117)return;window.__radarAlternativesV117=true;
const WORKER='https://radar-seguro-ia-rj.claudio41cg.workers.dev',SRC='route-alt-v117',CASING='route-alt-v117-casing',LINE='route-alt-v117-line',LABEL='route-alt-v117-label';
let timer=null,current=[],lastAnnounced='';
function getApp(){try{if(typeof App!=='undefined'&&App)return App;}catch(_){}return window.App||null;}
function getVoice(){try{if(typeof Voice!=='undefined'&&Voice)return Voice;}catch(_){}return window.Voice||null;}
function clear(){const map=getApp()?.map;if(!map)return;for(const id of [LABEL,LINE,CASING])try{if(map.getLayer(id))map.removeLayer(id);}catch(_){}try{if(map.getSource(SRC))map.removeSource(SRC);}catch(_){}current=[];}
function routeCoords(rr){const out=[];for(const leg of rr?.legs||[]){for(const p of leg?.points||[]){const c=[Number(p.longitude),Number(p.latitude)];if(!c.every(Number.isFinite))continue;const last=out[out.length-1];if(last&&Math.abs(last[0]-c[0])<1e-9&&Math.abs(last[1]-c[1])<1e-9)continue;out.push(c);}}return out;}
function distanceKm(a,b){const R=6371,r=x=>x*Math.PI/180,dLat=r(b[1]-a[1]),dLon=r(b[0]-a[0]);const q=Math.sin(dLat/2)**2+Math.cos(r(a[1]))*Math.cos(r(b[1]))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(q));}
function routeSimilarity(coords,active){if(!Array.isArray(coords)||!Array.isArray(active)||coords.length<2||active.length<2)return 999;const samples=12;let sum=0;for(let s=0;s<samples;s++){const p=coords[Math.floor((coords.length-1)*s/(samples-1))];let best=Infinity;for(let j=0;j<active.length;j+=Math.max(1,Math.floor(active.length/120))){best=Math.min(best,distanceKm(p,active[j]));}sum+=best;}return sum/samples;}
async function fetchAlternatives(){const a=getApp();if(!a?.route?.coords?.length||!a?.userPos||!a?.destination)return[];const start=a.userPos,end=Array.isArray(a.destination)?a.destination:[a.destination.lon??a.destination.lng,a.destination.lat];if(!Array.isArray(end)||end.length<2)return[];const points=`${Number(start[1]).toFixed(6)},${Number(start[0]).toFixed(6)}:${Number(end[1]).toFixed(6)},${Number(end[0]).toFixed(6)}`;const path=`/routing/1/calculateRoute/${points}/json?traffic=true&routeType=fastest&travelMode=car&routeRepresentation=polyline&maxAlternatives=2&computeTravelTimeFor=all&language=pt-BR`;try{const r=await fetch(`${WORKER}/v1/tomtom?path=${encodeURIComponent(path)}`,{cache:'no-store'});if(!r.ok)return[];const d=await r.json(),routes=d?.routes||[],active=a.route.coords;const all=[];for(let i=0;i<routes.length;i++){const rr=routes[i],coords=routeCoords(rr),sec=Number(rr?.summary?.travelTimeInSeconds),meters=Number(rr?.summary?.lengthInMeters);if(coords.length<2||!Number.isFinite(sec))continue;all.push({id:String(i),rr,coords,sec,minutes:Math.max(1,Math.round(sec/60)),km:Number.isFinite(meters)?meters/1000:null,similarity:routeSimilarity(coords,active),mid:coords[Math.floor(coords.length/2)]});}all.sort((x,y)=>x.sec-y.sec);const filtered=all.filter(x=>x.similarity>0.18);return (filtered.length?filtered:all.slice(1)).slice(0,3);}catch(e){console.warn('Radar v117 alternativas:',e);return[];}}
function draw(items){const a=getApp(),map=a?.map;if(!map)return;clear();current=items;if(!items.length)return;const features=[];for(const alt of items){features.push({type:'Feature',properties:{kind:'line',routeId:alt.id},geometry:{type:'LineString',coordinates:alt.coords}});features.push({type:'Feature',properties:{kind:'label',routeId:alt.id,text:`${alt.minutes} min${alt.km?` • ${alt.km.toFixed(0)} km`:''} • toque`},geometry:{type:'Point',coordinates:alt.mid}});}const data={type:'FeatureCollection',features};try{map.addSource(SRC,{type:'geojson',data});map.addLayer({id:CASING,type:'line',source:SRC,filter:['==',['get','kind'],'line'],layout:{'line-join':'round','line-cap':'round'},paint:{'line-color':'#ffffff','line-width':['interpolate',['linear'],['zoom'],9,6,13,9,16,12],'line-opacity':0.82}});map.addLayer({id:LINE,type:'line',source:SRC,filter:['==',['get','kind'],'line'],layout:{'line-join':'round','line-cap':'round'},paint:{'line-color':'#64748b','line-width':['interpolate',['linear'],['zoom'],9,3.5,13,5.5,16,7.5],'line-opacity':0.88}});map.addLayer({id:LABEL,type:'symbol',source:SRC,filter:['==',['get','kind'],'label'],layout:{'text-field':['get','text'],'text-size':13,'text-font':['Noto Sans Regular'],'text-allow-overlap':true},paint:{'text-color':'#0f172a','text-halo-color':'#ffffff','text-halo-width':3}});try{window.RadarRouteTrafficV74?.refresh?.();}catch(_){}}catch(e){console.warn('Radar v117 desenhar alternativas:',e);}}
async function chooseById(id){const alt=current.find(x=>x.id===String(id));if(!alt)return;const via=alt.coords[Math.floor(alt.coords.length/2)];if(!via)return;clear();const ok=await window.RadarRouteViaV115?.useViaPoint?.({lat:Number(via[1]),lon:Number(via[0]),label:'rota alternativa selecionada'});if(!ok)getVoice()?.speak?.('Não consegui trocar a rota agora. Tente novamente em alguns segundos.',true);}
function bind(){const map=getApp()?.map;if(!map||map.__routeAltV117Bound)return;map.__routeAltV117Bound=true;const click=e=>{const f=e?.features?.[0],id=f?.properties?.routeId;if(id!=null)chooseById(id);};for(const id of [LINE,LABEL]){map.on?.('click',id,click);map.on?.('mouseenter',id,()=>{try{map.getCanvas().style.cursor='pointer';}catch(_){}});map.on?.('mouseleave',id,()=>{try{map.getCanvas().style.cursor='';}catch(_){}});}}
async function refresh(announce=false){const a=getApp();if(!a?.route?.coords?.length){clear();return;}const items=await fetchAlternatives();draw(items);bind();if(announce&&items.length){const key=items.map(x=>`${x.minutes}-${x.km?.toFixed(1)||''}`).join('|');if(key!==lastAnnounced){lastAnnounced=key;setTimeout(()=>getVoice()?.speak?.(`Mostrei ${items.length} rota${items.length>1?'s':''} alternativa${items.length>1?'s':''} no mapa. Você pode tocar em qualquer uma para trocar.`,true),500);}}}
function install(){const a=getApp();if(!a||a.__routeAltV117Installed)return false;a.__routeAltV117Installed=true;const drawRoute=typeof a.drawRoute==='function'?a.drawRoute.bind(a):null;if(drawRoute)a.drawRoute=function(route,fit){const out=drawRoute(route,fit);setTimeout(()=>refresh(false),1200);return out;};bind();if(a.route?.coords?.length)setTimeout(()=>refresh(false),1800);clearInterval(timer);timer=setInterval(()=>{if(document.visibilityState==='visible'&&a.route?.coords?.length)refresh(false);},90000);return true;}
let tries=0;const boot=setInterval(()=>{tries++;if(install()||tries>100)clearInterval(boot);},200);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')setTimeout(()=>refresh(false),700);});window.RadarRouteAlternativesV116={version:'117-multi-alternatives',refresh:()=>refresh(true),clear,chooseCurrent:()=>current[0]&&chooseById(current[0].id)};})();